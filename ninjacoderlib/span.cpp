#include <booster>
#include <booster/ff.hh>
using namespace ff;
using namespace path;

 int main()
 {
   if(!Dictatory path = p)
    path p ()
     (p)(Dictatory)Url.const(Context_PSP_FILE)
     Url = "/sdcard/psp"
     return Url;
     }
     {
     
     int c = 0;
     bool ma = -80;
     define vbr = @null;
     Context_PSP_FILE -> FILERUDERPATH.GET()
     ASSTER->UI.ZIM()
     }
     #endif