#include <test.h>
#include <string>
#include <main.cpp>
#include <main.c>
#include <pspsdk.cpp>
#include<import.h>

using namespace srt;
using namespace srt2;
using namespace verical;
using namespace horsintal;
using namespace textview;
using namespace edittext;
using namespace listview;
using namespace drawel;
using namespace main;

int main()
{
    if new==srt1;
    filedir:("c/psp/");
    filedir:("c/psp%Game%savedata%")
    filedir:("c/psp%textures")
    }else{
           show.massges("filed,to,make,folder/n");
     };   
        
}
{
    string new == new.folder.setTimeout;
    new timer = timerask("seemaketofolder")
    file== timerask(3000);
    if{
        show.massges("ok");
        
    }
    
    listview1== set <arrylistmap>
    listmap;
    listmap== onChange("set Pathdir");
    listmap== onbindcustomview("/sdcard/");
    onbindcustomview(get.textview1("roda.filepath")))));
    onbindcustomview(Glide.with(getApplicationContext()).load(Uri.parse(_data.get((int)_position).get.toString())).into(img_content);
    
   android.on();
   set.pathfile();
}
